{
  "name": "backrooms-online",
  "version": "0.1.0",
  "private": true,
  "description": "Backrooms Online - Explore the backrooms in your browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
